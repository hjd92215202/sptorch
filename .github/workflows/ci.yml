name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --exclude runtime-cuda --exclude cli-train-gpu --exclude distributed -- -D warnings
      - name: Build mock-npu
        run: cargo build -p mock-npu
      - name: Test
        run: cargo test --workspace --exclude runtime-cuda --exclude cli-train-gpu --exclude distributed
