[package]
name = "core-ops"
version = "0.1.0"
edition = "2021"

[features]
cuda = ["runtime-cuda"]

[dependencies]
core-tensor = { version = "0.1.0", path = "../core-tensor" }
runtime-cuda = { version = "0.1.0", path = "../runtime-cuda", optional = true }
matrixmultiply = "0.3"
